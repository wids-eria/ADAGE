- @organizations.each do |org|
  .row.games
    .container.full.left
      .element
        .title.left 
          = org.name
        = link_to '', new_game_path(organization: org), class:'right fa fa-plus-square fa-2x '

    - org.games.each do |game|
      = render "games/preview", game: game