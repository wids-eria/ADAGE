%ul
  = @game.name
  %br
  = @game.created_at

  - @game.implementations.each do |s|
    %li
      = s.name
  = render partial: 'implementations/add', locals: {game: @game} 
  
  - @users.each do |user|
    %li
      = link_to user do 
        = user.player_name
        = user.consented 

