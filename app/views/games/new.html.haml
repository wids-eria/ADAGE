.row
  .container.full.left
    .element
      .title Create New Game
      = form_for @game do |game|
        %p
        = game.label :name
        = game.text_field :name
        = game.text_field :organization_id, value: params[:organization]
        %p
        = game.submit 'Add'